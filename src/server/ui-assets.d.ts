// Type companion for the generated ui-assets.ts (scripts/embed-ui.ts).
// The .ts file is gitignored because it embeds dist/ output; when it is
// absent (fresh clone before `bun run build:ui`), static.ts catches the
// failed import and serves from disk instead.
export declare const uiAssets: Record<string, string>;
