// Compile-time feature gates defined in bunfig.toml [define] — typed here so
// the gates are checked without a runtime flag object (project-rules rule 4).
declare const DECK_FEATURE_GROOMING: boolean;
declare const DECK_FEATURE_SSE: boolean;
declare const DECK_FEATURE_TWEAK: boolean;
