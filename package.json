{
  "name": "@dgtalbug/deck",
  "type": "module",
  "scripts": {
    "dev": "bun run src/server/serve.ts",
    "seed": "bun run scripts/seed.ts",
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "lint": "oxlint src tests scripts",
    "build:ui": "rm -rf dist/ui && bun build src/ui/app.tsx --outdir dist/ui --minify --splitting && bun run scripts/check-ui-budget.ts && bun run scripts/embed-ui.ts",
    "build:bin": "bun run build:ui && bun run scripts/embed-migrations.ts && bun build --compile --define DECK_FEATURE_GROOMING:true --define DECK_FEATURE_SSE:true --define DECK_FEATURE_TWEAK:true src/server/serve.ts --outfile deck",
    "build": "bun run build:bin",
    "start": "./deck",
    "deck": "bun run build:bin && ./deck"
  },
  "dependencies": {
    "@atlaskit/pragmatic-drag-and-drop": "^3.0.0",
    "@preact/signals": "^2",
    "dompurify": "^3.4.14",
    "drizzle-orm": "rc",
    "lucide-preact": "^1.34.0",
    "marked": "^18.0.11",
    "preact": "^10.29.8",
    "zod": "^4"
  },
  "devDependencies": {
    "bun-types": "latest",
    "drizzle-kit": "rc",
    "happy-dom": "^20.11.6",
    "oxlint": "latest",
    "swagger-ui-dist": "^5.32.14",
    "typescript": "^5"
  }
}
